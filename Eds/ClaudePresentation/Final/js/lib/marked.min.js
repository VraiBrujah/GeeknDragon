/**
 * Marked.js (version simplifiée pour démo)
 * Parseur Markdown minimaliste
 */

window.marked = {
    /**
     * Parse le markdown en HTML.
     */
    parse: function(markdown) {
        if (!markdown) return '';
        
        return markdown
            // Titres
            .replace(/^# (.*$)/gim, '<h1>$1</h1>')
            .replace(/^## (.*$)/gim, '<h2>$1</h2>')
            .replace(/^### (.*$)/gim, '<h3>$1</h3>')
            
            // Gras et italique
            .replace(/\*\*(.*?)\*\*/gim, '<strong>$1</strong>')
            .replace/\*(.*?)\*/gim, '<em>$1</em>')
            
            // Liens
            .replace(/\[([^\]]+)\]\(([^)]+)\)/gim, '<a href="$2">$1</a>')
            
            // Sauts de ligne
            .replace(/\n/gim, '<br>');
    }
};

console.log('Marked.js (version simplifiée) chargé');